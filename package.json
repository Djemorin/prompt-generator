{
  "name": "generateur-prompts",
  "version": "1.0.0",
  "description": "Éditeur de prompts avec Ollama",
  "main": "main.js",
  "scripts": {
    "start": "node server/generate-prompts.js",
    "electron": "electron .",
    "package-win": "electron-packager . GenerateurPrompt --platform=win32 --arch=x64 --out=dist --overwrite --prune=true"
  },
  "devDependencies": {
    "electron": "^29.0.0",
    "electron-packager": "^17.1.1"
  },
  "dependencies": {}
}
